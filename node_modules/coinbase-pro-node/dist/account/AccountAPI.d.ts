import { AxiosInstance } from 'axios';
export interface Account {
    available: string;
    balance: string;
    currency: string;
    hold: string;
    id: string;
    profile_id: string;
}
export interface AccountHistory {
    amount: string;
    balance: string;
    created_at: string;
    details: AccountHistoryDetails;
    id: string;
    type: string;
}
export interface AccountHistoryDetails {
    order_id: string;
    product_id: string;
    trade_id: string;
}
export interface Hold {
    account_id: string;
    amount: string;
    created_at: string;
    id: string;
    ref: string;
    type: string;
    updated_at: string;
}
export declare class AccountAPI {
    private readonly apiClient;
    static readonly URL: {
        ACCOUNTS: string;
    };
    constructor(apiClient: AxiosInstance);
    getAccount(accountId: string): Promise<Account>;
    getAccountHistory(accountId: string): Promise<AccountHistory[]>;
    getHolds(accountId: string): Promise<Hold[]>;
    listAccounts(): Promise<Account[]>;
}
